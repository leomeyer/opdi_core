<project name="OPDI" default="createjar">    
  <property name="projectHome" location="." />
  <target name="createjar">
    <jar destfile="${projectHome}/opdi.jar" basedir="${projectHome}/bin" />
  </target>    
</project>